<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Khohararik</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/lipis/flag-icons@7.2.3/css/flag-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        :root { --primary: #2d6a4f; --primary-light: #52b788; --accent: #f77f00; }
        body { background: #f8f9fa; display: flex; flex-direction: column; min-height: 100vh; }
        .navbar-brand { font-weight: 700; color: var(--primary) !important; font-size: 1.4rem; }
        .navbar { background: #fff !important; border-bottom: 2px solid var(--primary-light); }
        .nav-link:hover { color: var(--primary) !important; }
        footer { background: var(--primary); color: #fff; padding: 1.5rem 0; margin-top: auto; }
        footer a { color: var(--primary-light); }
        .cart-badge { position: relative; }
        .cart-badge .badge { position: absolute; top: -6px; right: -10px; font-size: 0.65rem; }
        /* Login / Register card styling */
        .auth-card { max-width: 480px; margin: 3rem auto; }
        .auth-card .card { border: none; box-shadow: 0 4px 24px rgba(0,0,0,.10); border-radius: 1rem; }
        .auth-card .card-body { padding: 2.5rem; }
        .auth-card h1 { font-size: 1.8rem; color: var(--primary); font-weight: 700; margin-bottom: 1.5rem; }
        .auth-card .btn-primary { background: var(--primary); border-color: var(--primary); }
        .auth-card .btn-primary:hover { background: var(--primary-light); border-color: var(--primary-light); }
        .auth-card a { color: var(--primary); }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" href="/">🥘 Khohararik</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarMain">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link" href="/Ingredients"><i class="bi bi-grid"></i> @L["Nav_Ingredients"]</a></li>
                        <li class="nav-item"><a class="nav-link" href="/Recipes"><i class="bi bi-journal-richtext"></i> @L["Nav_Recipes"]</a></li>
                        <li class="nav-item"><a class="nav-link fw-semibold text-success" href="/Recipes/Recommendations"><i class="bi bi-stars"></i> @L["Nav_GetRecommendations"]</a></li>
                    </ul>
                                        <div class="d-flex align-items-center gap-2">
                        <a href="/Cart" class="btn btn-outline-success cart-badge position-relative">
                            <i class="bi bi-cart3"></i>
                            <span class="badge bg-danger" id="cart-count">0</span>
                        </a>
                        <partial name="_LoginPartial" />
                        <div class="d-flex gap-1 ms-1">
                            <form action="/Culture/Set" method="post">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="culture" value="en" />
                                <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                <button type="submit" class="btn btn-sm px-1 py-0 border-0 bg-transparent" title="English"><span class="fi fi-gb fis" style="font-size:1.4rem; border-radius:3px; display:inline-block;"></span></button>
                            </form>
                            <form action="/Culture/Set" method="post">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="culture" value="hy" />
                                <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                <button type="submit" class="btn btn-sm px-1 py-0 border-0 bg-transparent" title="Հայերեն"><span class="fi fi-am fis" style="font-size:1.4rem; border-radius:3px; display:inline-block;"></span></button>
                            </form>
                            <form action="/Culture/Set" method="post">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="culture" value="ru" />
                                <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                <button type="submit" class="btn btn-sm px-1 py-0 border-0 bg-transparent" title="Русский"><span class="fi fi-ru fis" style="font-size:1.4rem; border-radius:3px; display:inline-block;"></span></button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <div class="container py-4 flex-grow-1">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <footer>
        <div class="container text-center">
            <p class="mb-0">@L["Footer_Copy"]</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        fetch('/Cart/Count').then(r => r.json()).then(d => {
            document.getElementById('cart-count').textContent = d.count;
        }).catch(() => {});
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
